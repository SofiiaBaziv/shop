import { SearchOrdPipe } from './search-ord.pipe';

describe('SearchOrdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOrdPipe();
    expect(pipe).toBeTruthy();
  });
});
