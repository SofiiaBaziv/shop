import { SearchCatPipe } from './search-cat.pipe';

describe('SearchCatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCatPipe();
    expect(pipe).toBeTruthy();
  });
});
